import HomeScreen from '@/app/components/home/HomeScreen';

/**
 * Home page — renders the XSMB Home Screen.
 * Server Component: just mounts the Client Component.
 */
export default function HomePage() {
  return <HomeScreen />;
}
